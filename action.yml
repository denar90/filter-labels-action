name: "PR filter labels"
description: "Filter labels"
inputs:
  filter:
    description: "List of labels"
    required: true
runs:
  using: "node12"
  main: "dist/index.js"
branding:
  icon: "git-branch"
  color: "gray-dark"
